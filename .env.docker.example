# Minimal Docker env for Portainer deploy

# Image + runtime paths
SA_HELPER_IMAGE=ghcr.io/jack101a/sa-helper:latest
API_PORT=8088
COMPOSE_PROJECT_NAME=sahelper
PUID=1001
PGID=1001
SA_RUNTIME_ROOT=./runtime/sa_helper

# Required security
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
ADMIN_TOKEN=
AUTH_HASH_SALT=

# Required Postgres
POSTGRES_DB=sa_helper
POSTGRES_USER=sa_helper
POSTGRES_PASSWORD=
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
RUN_ALEMBIC_MIGRATIONS=true

# Required Telegram bot
TELEGRAM_BOT_TOKEN=
TELEGRAM_API_BASE_URL=https://api.telegram.org

# Optional AI endpoint
LITELLM_ENDPOINT=
LITELLM_API_KEY=

# Optional scale mode (fullscale compose)
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
REDIS_MAXMEMORY=512mb
WORKER_CONCURRENCY=2
WORKER_SOLVE_BACKEND=celery_sync
SOLVE_BACKEND=celery_sync

# Optional backup overrides (defaults exist in app)
BACKUP_AUTO_ENABLED=false
BACKUP_ENCRYPTION_METHOD=age
BACKUP_AGE_RECIPIENT=
BACKUP_TELEGRAM_CHAT_ID=
BACKUP_RCLONE_DESTINATION=
