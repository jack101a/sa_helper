# tata admin-ui

React admin dashboard served by the FastAPI backend.

## Run Locally

```powershell
cd backend/admin-ui
npm install
npm run dev
```

## Build

```powershell
cd backend/admin-ui
npm run build
```

FastAPI serves the built files from `backend/admin-ui/dist/`.

